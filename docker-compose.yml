version: '3.8'

services:
  app:
    image: ghcr.io/DavidVMuela/muela:1.0.5
    ports:
      - "5000:5000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    networks:
      - traefik-public
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.muela.rule=Host(`muela.byronrm.com`)"
        - "traefik.http.routers.muela.entrypoints=websecure"
        - "traefik.http.routers.muela.tls.certresolver=letsencrypt"
        - "traefik.http.services.muela.loadbalancer.server.port=5000"

networks:
  traefik-public:
    external: true